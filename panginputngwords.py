

#from main import *
#import numpy as np

#actions = np.array(['Merry Christmas', 'Kamusta' ])
#actions = np.array([])

# Use a while loop to get input from the user
#while True:
    # Get input from the user
#    user_input =   input("Enter The word you want to train: ")#self.text_input_pangwords.toPlainText("Enter The word you want to train: ")
    
    # If the user enters 'done', exit the loop
#    if user_input.lower() == 'done':
#        break
    
    # Otherwise, append the user's input to the array
#   actions = np.append(actions, user_input)

#print(actions)  # Output: [user_input1, user_input2, ...]